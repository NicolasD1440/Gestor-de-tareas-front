version: '3.8'

services:
  frontend:
    build: .
    container_name: task-manager-frontend

    ports:
      - "5173:5173"

    volumes:
      - .:/app
      - /app/node_modules

    stdin_open: true
    tty: true